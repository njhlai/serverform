---
# diskconf: Role for disks configuration
- name: Import disk-burnin script installation tasks
  ansible.builtin.import_tasks:
    file: install.yml

- name: Configure disks
  ansible.builtin.import_tasks:
    file: config.yml
  when: diskconfig